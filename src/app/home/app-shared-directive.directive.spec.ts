import { AppSharedDirectiveDirective } from './app-shared-directive.directive';

describe('AppSharedDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AppSharedDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
